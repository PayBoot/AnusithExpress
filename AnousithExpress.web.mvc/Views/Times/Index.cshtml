@model IEnumerable<AnousithExpress.DataEntry.Models.TbTime>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_CustomerLayout.cshtml";
}

<h2>ເວລາສົ່ງ</h2>

<p>
    @Html.ActionLink("ສ້າງໃຫມ່", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Time)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Time)
        </td>
        <td>
            @Html.ActionLink("ປ່ຽນແປງ", "Edit", new { id=item.Id }) |
            @Html.ActionLink("ລາຍລະອຽດ", "Details", new { id=item.Id }) |

        </td>
    </tr>
}

</table>
